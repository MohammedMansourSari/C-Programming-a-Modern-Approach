#include <stdio.h>

int main(void)
{
    printf("%zu %zu %zu %zu %zu %zu %zu", sizeof(short), sizeof(int), sizeof(long), sizeof(long long), sizeof(float), sizeof(double), sizeof(long double));

    return 0;
}
